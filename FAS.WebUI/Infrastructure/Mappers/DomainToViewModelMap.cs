using AutoMapper;
using FAS.Domain;
using FAS.WebUI.Models;

namespace FAS.WebUI.Infrastructure.Mappers
{
    public class DomainToViewModelMap
    {
        public static void CreateMap(IMapperConfigurationExpression config)
        {
            //config.CreateMap<Book, SimpleBookViewModel>();
            //config.CreateMap<Book, CreateBookViewModel>();
            //config.CreateMap<Book, ChangeBookViewModel>();
        }
    }
}