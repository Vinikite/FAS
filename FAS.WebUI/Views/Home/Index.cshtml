@model List<FAS.Domain.Score>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="text-info h1">Score</div>
@if (Model.Count == 0)
{
    <div class="text-danger h3">No items</div>
}
else
{
    @*<table class="table table-striped">
        <thead>
            <tr>
                <td>Name</td>
                <td>Price</td>
                <td>CreateOn</td>
                <td>ModifyOn</td>
                <td>IsDeleted</td>
                <td>DeleteOn</td>
                <td></td>
            </tr>
        </thead>
        <tbody>
            @foreach (var book in Model)
            {
                <tr>
                    <td>@book.Name</td>
                    <td>$@book.Price</td>
                    <td>@book.CreateOn</td>
                    <td>@book.ModifyOn</td>
                    <td>@book.IsDeleted</td>
                    <td>@book.DeleteOn</td>
                    <td>
                        @if (!book.IsDeleted)
                        {
                            <div class="btn-group">
                                <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    Action <span class="caret"></span>
                                </button>
                                <ul class="dropdown-menu">
                                
                                        <li>
                                            <a href="@Url.Action("Change", "Home", new { id = book.Id })">
                                                <span class="glyphicon glyphicon-pencil"></span> Change
                                            </a>
                                        </li>
                                    @if (HttpContext.Current.Request.IsAuthenticated)
                                    {
                                        <li>
                                            <a href="@Url.Action("Delete", "Home", new { id = book.Id })">
                                                <span class="glyphicon glyphicon-floppy-remove"></span> Remove
                                            </a>
                                        </li>
                                    }
                                </ul>
                            </div>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>*@
}

<a class="btn btn-success" href="@Url.Action("Create", "Home")"><span class="glyphicon glyphicon-plus"></span> Create</a>


